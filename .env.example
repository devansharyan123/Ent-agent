# ─── .env.example ────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your actual values.
# The .env file is gitignored and must NEVER be pushed to GitHub.
#
# Usage:
#   cp .env.example .env
#   # then edit .env with your real keys

GROQ_API_KEY=your_groq_api_key_here
